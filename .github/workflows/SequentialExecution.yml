name: Sequential Execution 
on: workflow_dispatch
jobs:
  FirstJob:
    runs-on: [dev]
    steps:
      - name: First Job Executing
        run: echo "Executed"
  SecondJob:
    needs: [FirstJob]
    runs-on: [dev]
    steps:
      - name: Second Job Executing
        run: echo "done"
